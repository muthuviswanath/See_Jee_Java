//Identifier
/*
 * 1. It cannot start with a number but can contain numbers
 * 2. It cannot contain white space between words.
 * 3. Only $ and _ are acceptable symbols
 * 4. Cannot be a Java Keyword
 * 
 * */
public class Box {
	/*
	 * shape dimensions color material price
	 */
	//Global Variable Syntax:
	// [access_specifier][access_modifier]<datatype><variable_name>[=<literal>];
	// public, protected, default, private  
	// final
	// primitive or non-primitive (class or interface)
	// variable_name is an identifier
	// literal = constant value assigned to a variable.
	public final double PIE = 3.1415;
		   final int HEIGHT = 8848;
		   		 boolean isTrue  = false;
		   		 long mobilenumber;
		   		 static int age = 45;
		  public static void main(String[] args) {
			System.out.println(age);
			test();
			new Box();
		}
		  
		  void display() {
			  System.out.println(age);  
		  }
		  static void test() {}
		  
	//global variable characteristics
	/*
	 * The scope is for the entire class
	 * Can be static
	 * can have access specifiers
	 * It is not mandatory to initialize the global variable, in which case, it takes
	 * default value based in the datatype
	 * 	byte, short, int, long - 0
	 *  float, double - 0.0
	 *  boolean - false
	 *  character - 
	 *  Non-primitive - Null
	 *  
	 *  
	 * */
	
}
